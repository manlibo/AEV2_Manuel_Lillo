{% extends "main.html.twig" %}

{% block container %}

    {% if results is null %}
        0 results
        <a href="/">Home</a>
    {% else %}
        <table>
            <th>
                <td>Description</td>
                <td>Warehouse</td>
                <td>Unit</td>
                <td>Rating</td>
                <td>Price/Unit</td>
            </th>
            {% for row in results %}
                <tr>
                    <td>{{row.getDescripcion()}}</td>
                    <td>{{row.getAlmacen()}}</td>
                    <td>{{row.getUnidad()}}</td>
                    <td>{{row.getClasificacion()}}</td>
                    <td>{{row.getPrecioUnidad()}}</td>
                </tr>
            {% endfor %}
        </table>
    {% endif %}

{% endblock container%}